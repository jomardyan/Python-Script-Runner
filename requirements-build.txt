# Python Script Runner - Build Requirements
# For building Windows EXE and Linux DEB packages
# Usage: pip install -r requirements-build.txt

# Base requirements (as fallback)
-r requirements.txt

# PyInstaller for building Windows EXE
pyinstaller>=6.0.0

# Additional build tools
setuptools>=65.0.0
wheel>=0.40.0

# Optional: Code signing tools
# pycodesign>=1.0.0  (for Windows EXE code signing)

# Optional: Package creation helpers
# fpm>=1.14.0  (for building RPM, DEB from Python packages)
# bdist-deb>=0.1.3  (alternative DEB builder)
